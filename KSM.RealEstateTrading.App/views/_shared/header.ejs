<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>

    <link rel="stylesheet" href="/stylesheets/style.css" />
    <% if (typeof styles !== 'undefined') { styles.forEach(function(style){  %>
    <link rel='stylesheet' href='<%= style %>' />
    <% }) } %>
  </head>
  <body>